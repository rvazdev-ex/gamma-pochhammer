% Web-only macros. The blueprint plasTeX plugin already defines the
% dependency-graph markers (\lean, \leanok, \uses, \proves, ...) so
% this file is typically empty. Add web-only LaTeX macros here if needed.
